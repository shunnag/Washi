# 課題の正本は embedded Dolt。コードの Git 履歴とは別に同期する。
sync.remote: "git+https://github.com/shunnag/Washi.git"
dolt.auto-commit: "off"

# 閲覧・移行用の JSONL を更新する。Git の自動ステージは行わない。
export:
  auto: true
  path: issues.jsonl
  interval: 60s
  git-add: false
